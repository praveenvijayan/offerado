import React from "react";

const GallerySingleTemplate = ({ offer }: { offer: any }) => {
  return (
    <div className="single-template default-template">
      <h1>Gallery template</h1>
    </div>
  );
};

export default GallerySingleTemplate;
